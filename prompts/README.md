# Prompt library

Reusable prompts, organized by topic. These are **not** auto-loaded. Open a file or `@`-mention it when you need it.

```text
prompts/
  review/
  research/
  writing/
```

Put one prompt per markdown file. Name the file after the job (`pr-review.md`, `literature-notes.md`), not after a model.
